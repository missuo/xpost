# API token for protecting /v1/tweets endpoint.
XPOST_API_TOKEN=replace-with-strong-token
XPOST_CONFIG=config.json

# OAuth2 app settings for CLI login.
X_OAUTH2_CLIENT_ID=
X_OAUTH2_CLIENT_SECRET=
X_OAUTH2_REDIRECT_URI=
X_OAUTH2_SCOPE=tweet.read,tweet.write,users.read,offline.access

# OAuth2 user token values (normally generated by `xpost login` and saved to config).
# X_OAUTH2_ACCESS_TOKEN=
# X_OAUTH2_REFRESH_TOKEN=
# X_OAUTH2_TOKEN_TYPE=Bearer
# X_OAUTH2_EXPIRES_AT=0

# OAuth1 is still supported as an alternative auth mode.
# X_API_KEY=
# X_API_SECRET=
# X_ACCESS_TOKEN=
# X_ACCESS_TOKEN_SECRET=

# User ID for timeline endpoint.
# X_USER_ID=
